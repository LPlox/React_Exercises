const TrueFalseState = () => {
  //Create an useState to store the true/false state.
  //Make the initial value false first.
  //Write out the result in html.

  return (
    <div>
      {/* Write the variable of the state below */}
      <p>{}</p>
      {/* Write a onClick function to change the state */}
      <button></button>
    </div>
  );
};

export default TrueFalseState;
